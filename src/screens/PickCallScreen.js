import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PickCallScreen = () => {
  return (
    <View style={styles.body}>
      <Text>PickCallScreen</Text>
    </View>
  );
};

export default PickCallScreen;

const styles = StyleSheet.create({
  body: {},
});
